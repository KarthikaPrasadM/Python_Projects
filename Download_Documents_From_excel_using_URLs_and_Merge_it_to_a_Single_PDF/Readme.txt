1. Created an application to download different kind of documents(PDF, Docx, JPG, JPEG, XLSX, XLS) using URLs from a xlsx.
   Also, convert and merge it to a single PDF according to the given row name.

Steps
1. "pip install -r requirements.txt"
2. Create a sample input in xlsx like the given image.